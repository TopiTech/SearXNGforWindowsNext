# Windows patches

## Source of truth
- Upstream: `searxng/searxng`
- Base fork: `mbaozi/SearXNGforWindows`
- Do not use `privau/searxng` as merge/sync source

## Current Windows compatibility patches
- `searx/valkeydb.py`
  - replace Unix-only `pwd` usage with Windows-safe fallback
  - avoid hard dependency on `os.getuid()` on Windows

## Sync policy
- Sync by commit SHA whenever possible
- Re-apply Windows patches after every upstream sync
- Record resolved upstream commit in `UPSTREAM_VERSION.txt`